# Apenas criando uma função para uso próprio e facilitar o código  ( exemplo )

import pprint


def p(v):
    pprint.pprint(v, sort_dicts=False, width=40)



p(novos_produtos), o conteúdo definido no argumento substituti o v

# Como definir tipo o parâmetro e argumento pra reutilizar 